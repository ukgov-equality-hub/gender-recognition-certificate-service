apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gender-recognition-service-admin
  namespace: grc-preprod
  annotations:
    nginx.ingress.kubernetes.io/whitelist-source-range: 81.134.202.29/32
    external-dns.alpha.kubernetes.io/set-identifier: gender-recognition-service-grc-preprod-green
    external-dns.alpha.kubernetes.io/aws-weight: "100"
spec:
  ingressClassName: default
  tls:
  - hosts:
    - preprod.admin.gender-recognition.service.justice.gov.uk
    secretName: gender-recognition-service-admin-secret

  rules:
  - host: preprod.admin.gender-recognition.service.justice.gov.uk
    http:
      paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: grc-admin-service
                port: 
                  number: 3001