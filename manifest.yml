---
applications:
  - name: geo-gender-recognition-certificate
    memory: 256M
    buildpacks:
    - python_buildpack
    env:
      AV_API: http://geo-gender-recognition-certificate-clamav.apps.internal:8080
    # TODO: better to manual bind this (cf bind-service geo-gender-recognition-certificate postgres-13-dev)
    # services:
    # - postgres-13-dev
    command:  ./prod.sh
