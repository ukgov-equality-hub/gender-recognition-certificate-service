---
applications:
  - name: grc-production-admin
    memory: 1G
    buildpacks:
    - python_buildpack
    # TODO: better to manual bind this (cf bind-service geo-gender-recognition-certificate postgres-13-dev)
    # services:
    # - postgres-13-dev
    command:  ./prod_admin.sh
