---
applications:
  - name: grc-staging
    memory: 256M
    buildpacks:
    - python_buildpack
    # TODO: better to manual bind this (cf bind-service geo-gender-recognition-certificate postgres-13-dev)
    # services:
    # - postgres-13-dev
    env:
      AV_API: http://grc-production-clamav.apps.internal:8080
    command:  ./prod.sh
