apiVersion: v1
kind: Service
metadata:
  name: grc-dashboard-service
  labels:
    app: grc-dashboard-service
spec:
  ports:
  - port: 4567
    name: http
    targetPort: 4567
  selector:
    app: grc-dashboard
